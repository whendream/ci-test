# 添加这一行，解决第二个 warning
requires-python = ">=3.12"

[project]
name = "fast-python-app"
version = "0.1.0"
dependencies = [
    "requests>=2.31.0",
]

# 修改这里，解决第一个 warning
[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.3.0",
]