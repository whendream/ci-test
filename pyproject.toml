[project]
name = "fast-python-app"
version = "0.1.0"
description = "A fast Python app using uv and GitHub Actions"
dependencies = [
    "requests>=2.31.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
    "ruff>=0.3.0", # 超快的代码格式检查工具
]